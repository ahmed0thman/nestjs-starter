// ===================================
// User & Auth related enums
// ===================================

enum userStatus {
  PENDING_VERIFICATION
  ACTIVE
  INACTIVE
  SUSPENDED
  DELETED
  BLOCKED
}

enum mfaMethod {
  otp
  totp
  sms
  email
  webauthn
}

enum loginAction {
  login
  logout
}

enum verification_secret_status {
  fresh
  used
  expired
}

// ================================
// Email Related Enums
// ================================

enum email_type {
  verification
  password_reset
  notification
}

enum email_provider {
  sendgrid
  mailgun
  ses
  smtp
}

enum email_status {
  sent
  failed
  pending
  bounced
  delivered
  opened
  clicked
}
